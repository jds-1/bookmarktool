# Environment Configuration for Bookmark Manager
# Copy this file to .env and customize as needed

# === Storage Configuration ===
# Path to bookmarks YAML file (default: ./bookmarks.yaml)
BOOKMARKS_PATH=/data/bookmarks.yaml

# Path to icons directory (default: same directory as bookmarks + /icons)
ICONS_PATH=/data/icons

# === Flask Configuration ===
# Server host (default: 0.0.0.0)
FLASK_HOST=0.0.0.0

# Server port (default: 5000)
FLASK_PORT=5000

# Debug mode (default: false for production)
FLASK_DEBUG=false

# Flask environment (development/production)
FLASK_ENV=production

# === Docker Swarm Configuration ===
# Domain for Traefik routing (update this!)
BOOKMARK_DOMAIN=bookmarks.yourdomain.com

# Data directory on host (update this!)
BOOKMARK_DATA_DIR=/opt/bookmark-data